[project]
name = "bitrix-fastapi"
version = "0.1.0"
description = "Integración Bitrix24 con FastAPI"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
]
